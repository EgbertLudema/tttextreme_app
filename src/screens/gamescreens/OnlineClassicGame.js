import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const OnlineClassicGame = () => {
    return (
        <View style={styles.container}>
            <Text>Online Classic Game</Text>
            {/* Replace with your online classic game component */}
        </View>
    );
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});

export default OnlineClassicGame;
