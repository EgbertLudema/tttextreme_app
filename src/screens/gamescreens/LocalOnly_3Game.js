import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const LocalOnly_3Game = () => {
    return (
        <View style={styles.container}>
            <Text>Local Only 3 Game</Text>
            {/* Replace with your online extreme game component */}
        </View>
    );
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});

export default LocalOnly_3Game;
