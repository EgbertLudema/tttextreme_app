import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const OnlineExtremeGame = () => {
    return (
        <View style={styles.container}>
            <Text>Online Extreme Game</Text>
            {/* Replace with your online extreme game component */}
        </View>
    );
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});

export default OnlineExtremeGame;
