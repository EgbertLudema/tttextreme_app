import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const OnlineOnly_3Game = () => {
    return (
        <View style={styles.container}>
            <Text>Online Only 3 Game</Text>
            {/* Replace with your online extreme game component */}
        </View>
    );
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});

export default OnlineOnly_3Game;
